{
  "name": "editplus",
  "version": "0.1.0",
  "description": "Tag-based file read, edit, and search tools for AI agents. Concurrency-safe parallel edits with stale detection, structural code summaries via tree-sitter, and stable cross-file tag addressing — no fragile text matching needed. First-class pi/GSD extension.",
  "type": "module",
  "main": "./index.js",
  "exports": "./index.js",
  "scripts": {
    "test": "node --test test/*.test.mjs"
  },
  "gsd": {
    "extension": true
  },
  "pi": {
    "extensions": [
      "index.js"
    ]
  },
  "dependencies": {
    "@kreuzberg/tree-sitter-language-pack": "^1.6.2"
  }
}
